server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/t_user?characterEncoding=utf8&useSSL=true
    driverClassName: com.mysql.jdbc.Driver
    username: root
    password: 19900908
  jpa:
      database: MYSQL
      show-sql: true
      hibernate:
        naming_strategy: org.hibernate.cfg.ImprovedNamingStrategy
